name: Check Signal Outcomes
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Ping check-signals endpoint
        run: curl -sf "https://xautracker.com/api/check-signals?secret=${{ secrets.CRON_SECRET }}"
